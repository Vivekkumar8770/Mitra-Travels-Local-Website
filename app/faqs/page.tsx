import type { Metadata } from "next";
import Link from "next/link";
import { MessageCircle } from "lucide-react";
import { Accordion, AccordionContent, AccordionItem, AccordionTrigger } from "@/components/ui/accordion";
import { getPublicFaqs } from "@/lib/store";

export const metadata: Metadata = { title: "Frequently Asked Questions", description: "Answers about Mitra Travels packages, customisation, hotels, quotations and India–Nepal tours." };
export default async function FaqPage() { const faqs = await getPublicFaqs(); return <main><section className="page-hero"><div className="container-shell page-hero-inner"><p className="eyebrow text-orange-300">Before you travel</p><h1 className="mt-4 max-w-4xl text-4xl font-extrabold tracking-tight sm:text-6xl">Frequently asked questions</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-white/70">Clear answers about custom packages, hotels, quotations and the enquiry process.</p></div></section><section className="section-space"><div className="container-shell grid gap-10 lg:grid-cols-[0.7fr_1.3fr]"><div><p className="section-kicker">Need help?</p><h2 className="mt-3 text-3xl font-extrabold text-slate-950">Speak directly with our travel team</h2><p className="mt-4 leading-7 text-slate-600">If your question is not listed here, send us your route and travel dates. We’ll help you plan the right option.</p><Link href="/contact#enquiry" className="btn btn-primary mt-7">Ask a question <MessageCircle className="size-4" /></Link></div><div className="surface px-6 sm:px-8"><Accordion type="single" collapsible>{faqs.map((item, index) => <AccordionItem key={item.question} value={`faq-${index}`}><AccordionTrigger className="py-6 text-base font-bold no-underline hover:no-underline">{item.question}</AccordionTrigger><AccordionContent className="pb-6 text-base leading-7 text-slate-600">{item.answer}</AccordionContent></AccordionItem>)}</Accordion></div></div></section></main>; }
